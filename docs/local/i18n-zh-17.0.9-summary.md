# i18n 跟版 17.0.9 + 全量补译摘要

> **日期**: 2026-07-23  
> **版本**: 17.0.8 → 17.0.9  
> **执行**: MiMo

---

## Phase 0 — 备份

备份至 `lang/_backup/2026-07-23-pre-17.0.9/`，包含全部 zh-*.json + en-*.json。

## Phase 1 — 上游 17.0.9 EN 同步

**方法**: `gh api repos/can1357/oh-my-pi/compare/v17.0.8...v17.0.9` 对比发现 **en-*.json lang 文件无变化**。v17.0.9 的 i18n 相关变更在 `settings-schema.ts` 中新增了两个设置项（inline English），未反映在 en-*.json SoT 文件中。

**手动补充 en-*.json**:
- `en-settings-tools.json`: +`settings.mcp.renderMarkdownResults.label/description`
- `en-settings-tasks.json`: +`settings.task.isolation.apply.label/description`

**package.json version**: 17.0.8 → 17.0.9 ✓

## Phase 2 — Key 对齐

所有 en-*.json 与 zh-*.json key 集完全一致（settings 10 分类 + commands）。

| 文件 | EN keys | ZH keys | 状态 |
|---|---|---|---|
| en-settings-appearance.json | 同 | 同 | ✓ |
| en-settings-context.json | 同 | 同 | ✓ |
| en-settings-files.json | 同 | 同 | ✓ |
| en-settings-interaction.json | 同 | 同 | ✓ |
| en-settings-memory.json | 同 | 同 | ✓ |
| en-settings-model.json | 同 | 同 | ✓ |
| en-settings-providers.json | 同 | 同 | ✓ |
| en-settings-shell.json | 同 | 同 | ✓ |
| en-settings-tasks.json | 同 | 同 | ✓ |
| en-settings-tools.json | 同 | 同 | ✓ |
| en-commands.json | 同 | 同 | ✓ |

## Phase 3 — 全量质量扫

**扫描方法**: 逐 key 对比 en/zh 值，检查:
1. zh 缺失或空串 → 0
2. zh==en 且非白名单 → 0（经白名单过滤后）
3. zh 无汉字且含空格长句 → 0
4. 占位符 `{x}` / `%s` 一致 → ✓

**白名单放行项** (148 条，均为合法英文):
- 数值标签: `0.2`, `0.5`, `256`, `512` 等
- 百分比: `10%`~`95%`
- 技术术语: `Unicode`, `ASCII`, `Powerline`, `LSP`, `TTSR`, `APFS`, `ZFS`, `btrfs`, `Reflink`, `Overlayfs`, `ProjFS`, `Bash`, `Shell`, `Agent`, `Git`, `Mnemopi`
- 品牌名: `OpenAI`, `Anthropic`, `DeepSeek`, `Gemini`, `Gemma`, `Hermes`, `Kimi`, `GLM`, `Qwen3`, `MiniMax`, `Harmony`, `OpenRouter`, `Antigravity`, `Trafilatura`, `Lynx`, `Jina`, `Exa Websets`, `GitHub`, `Fireworks`
- 协议/格式: `Native`, `XML`, `AGENTS.md`
- Token 估算: `~250 tokens`~`~250K tokens`
- 文件大小: `1 KB`~`200 KB`

**关键 tab 抽检**:

### 任务 (tasks) — 隔离模式全部选项
| 选项 | label | description |
|---|---|---|
| None | 无 | 无隔离 |
| Auto | 自动 | 让 PAL 选择最佳可用后端 |
| APFS | APFS | macOS 克隆文件 reflink（APFS） |
| btrfs | btrfs | btrfs 子卷快照 |
| ZFS | ZFS | ZFS 快照 + 克隆 |
| Reflink | Reflink | Linux FICLONE 逐文件 reflink |
| Overlayfs | Overlayfs | Linux 内核 overlay（或 fuse-overlayfs 回退） |
| ProjFS | ProjFS | Windows 投影文件系统 |
| Block clone | 块克隆 | Windows 块克隆（NTFS/ReFS） |
| Recursive copy | 递归复制 | 优先 git worktree，否则递归复制 |

新 v17.0.9 设置:
- `task.isolation.apply`: 应用隔离变更 / 自动将成功的隔离任务变更应用到父检出；禁用以保留补丁或分支产物 ✓

### 提供商 (providers) — description 抽检
所有 description 均已中文，无英文长句残留。✓

### 插件 (plugins)
`tabs.plugins.label`: 插件 ✓

### MCP (tools)
新 v17.0.9 设置:
- `mcp.renderMarkdownResults`: MCP Markdown 结果 / 在对话记录中将非 JSON 的 MCP 文本结果渲染为 Markdown ✓

## Phase 4 — 壳层回归

- `interceptWelcomeString` 全 key 中文 ✓
- `ui.settings.title`: 设置 ✓
- footer 5 key 中文 ✓
- `tabs.plugins.label`: 插件 ✓

## Phase 5 — 门禁

| 门禁 | 状态 |
|---|---|
| package.json version = 17.0.9 | ✓ |
| 每文件 keys(zh)==keys(en) | ✓ |
| 无空串 | ✓ |
| 无重复 key | ✓ |
| 占位符一致 | ✓ |
| 非白名单 zh==en 句子 = 0 | ✓ |

## Phase 6 — 文件变更

| 文件 | 变更 |
|---|---|
| `packages/coding-agent/package.json` | version 17.0.8 → 17.0.9 |
| `en-settings-tools.json` | +2 keys (mcp.renderMarkdownResults) |
| `en-settings-tasks.json` | +2 keys (task.isolation.apply) |
| `zh-settings-tools.json` | +2 keys (中文翻译) |
| `zh-settings-tasks.json` | +2 keys (中文翻译) |
| `zh-settings-interaction.json` | 1 处修正: Power (macOS) → 电源 (macOS) |
| `zh-settings-shell.json` | 1 处修正: Eval & Runtimes → 执行与运行时 |
| `docs/local/i18n-zh-17.0.9-summary.md` | 本文件 |
| `lang/_backup/2026-07-23-pre-17.0.9/` | 备份 (30 文件) |

## 备注

- 上游 v17.0.9 的 en-*.json lang 文件与 v17.0.8 完全相同（`gh api compare` 确认）
- v17.0.9 新增的两个设置项定义在 `settings-schema.ts` 的 inline English 中，未导出到 en-*.json SoT 文件
- 已手动将这两个设置项的 i18n key 补入 en-*.json 和 zh-*.json，确保 i18n 拦截层可正确翻译
